# Ref: https://kind.sigs.k8s.io/docs/user/configuration/#extra-labels
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
- role: worker
  # extraPortMappings:
  # - containerPort: 30950
  #   hostPort: 80
  # labels:
  #   tier: tools
